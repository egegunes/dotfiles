alias rm="rm -Iv --preserve-root"
alias mv="mv -vi"
alias cp="cp -vi"
alias ln="ln -vi"
alias mkdir="mkdir -p"

alias du='du -kh'
alias df='df -kTh'

alias gpg="gpg2"

if [ -f ~/.local_aliases ]; then
    source ~/.local_aliases
fi
